# zall — Release Notes (0.6.1)

0.6.0 → 0.6.1 的变更。都是交互打磨与 bug 修复，无破坏性变更，配置
文件格式不变。技术细节见 `CHANGELOG.md`。

## 变更

- **`/provider` 改为菜单向导（Kimi CLI 风格）**：方向键 ↑↓ 选择平台，
  Enter 确认，选中即切换并自动落盘（此前交互接入不持久化，重启后网关
  消失）。选项显示 key 状态、端点、自定义标记。
- **「＋ 添加新网关」三步接入**：任何 OpenAI 兼容 API — 选来源（内置 14
  家目录或直接贴 URL）→ 粘贴 API key（隐藏输入，不回显）→ 自动探测
  模型列表并挑选 → 一起写进配置，重启还在。
- **密钥输入隐藏**：Windows 原生逐键读取（msvcrt），无回显，Enter 提交 /
  Esc 取消；修复了 REPL 内密钥明文泄漏的问题。
- 首次启动的引导改为一句话指引（"运行 /provider，选平台 → 贴 key → 选
  模型"），不再跳出三字段表单。

## 修复

- 交互接入的网关重启后消失（`persist=False`）→ 交互路径一律持久化。
- REPL 中粘贴 API key 可能明文落到任务输入框。
- 选择菜单按 ↑↓ 时高亮不动。

## 升级

```
pip install -U zall
```